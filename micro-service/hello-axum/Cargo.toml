[package]
name = "hello-axum"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.2"

serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"

tokio = {version = "1.0", features = ["full"]}

tracing = "0.1"
tracing-subscriber = "0.2"